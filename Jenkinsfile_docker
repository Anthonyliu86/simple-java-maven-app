pipeline {
    agent {
        label 'slave-1'
    }

    stages {
        stage('Build') {
            agent {
                docker { image 'maven' }
            }
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
       
       
    }
}
